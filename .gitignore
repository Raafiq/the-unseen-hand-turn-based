# Dependencies
node_modules/

# Build output
dist/
build/
out/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Test / coverage
coverage/
.nyc_output/

# Playwright / visual-test artifacts (regenerated in CI, deployed to Pages)
visual-artifacts/
test-results/
playwright-report/
.playwright/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# OS / editor junk
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code — personal, per-user settings (shared settings live in .claude/settings.json)
.claude/settings.local.json

# Claude Code — hook audit logs (local only)
.claude/hooks/*.log

# Spec Kit — additional ignore rules are added by `specify init` at P0.
