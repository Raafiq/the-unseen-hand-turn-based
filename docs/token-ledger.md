# Token ledger

One row per slice, written by the retrospective (`retrospective` skill, step 0) before the
PR opens. The pre-PR hook refuses a PR until this file has a row from the current session.
Numbers come from the harness's per-agent usage notices; the main session's own spend is an
estimate and says so. The point is the last two columns: name the waste, name the guard.

| Date | Slice | Total | Agents (tokens) | Biggest avoidable cost | Guard added |
| --- | --- | --- | --- | --- | --- |
| 2026-09-06 | Portrait wiring (ADR-0039) | ~945k (agents 853k, main ~90k est.) | viewer-engineer 246k, viewer-engineer 160k, reviewer 93k, art-director 89k, docs-steward 75k, docs-steward 75k, release-engineer 71k, claude-code-guide 45k | ~250k: a second engineer pass the first brief should have prevented (100k); docs briefed before the code constraint was checked, two passes (60k); art director over-delivered (40k); slot-id bug found by a browser run (30k); a doc-lookup agent where one WebFetch answered it (45k); a throwaway capture script because the slice never added the unit card to the playtest capture (30k) | `guard-brief.sh` (brief must name MUTATION, ASSERT, a report cap; one pass for art; one docs pass per session), effort floors per agent, spawn caps, `check:hooks` |
| 2026-09-06 | Title screen in the concept look (ADR-0040) + scene-player mockup | ~1.76M (agents 1.69M, main ~70k est.) | viewer-engineer 378k, viewer-engineer 249k, viewer-engineer 191k, art-director 187k, art-director 172k, viewer-engineer 168k, docs-steward 97k, docs-steward 83k, release-engineer 92k (45k + 47k resume), reviewer 77k | ~400k: a drift-fix pass the first engineer's "matches the mockup" claim hid (191k); docs run beside the review, second pass (83k); a label specified without a render, fixed after (~80k); a resumed release engineer that refused relayed words (47k) | `viewer-engineer.md`: reference frame = numeric diff; `src/render/CLAUDE.md`: CSS leak + control-state rules; root `CLAUDE.md`: docs after review, go-token facts; `release-engineer.md`: rejected-push rule |
| 2026-09-07 | Scene player in the concept look (ADR-0040 amendment, AC-V47..V50) | ~1.22M (agents 1.19M, main ~30k est.) | viewer-engineer 423k, viewer-engineer 239k, viewer-engineer 165k, reviewer 121k, docs-steward 99k, release-engineer 57k (PR, blocked by this gate), release-engineer 44k, release-engineer 44k | ~240k: a drift-fix pass that the numeric-diff rule did not prevent because the differing regions were labelled, not classified (239k) | `viewer-engineer.md`: every differing region is classified by name; chrome drift is fixed before the report |
| 2026-09-07 | Prep screen in the concept look (ADR-0040 second amendment, AC-V51..V59; owner option A: briefing + prep as one two-pane screen) | ~2.65M (agents 2.49M, main ~160k est.) | viewer-engineer 733k (port + 10 tests + captures + diff in ONE context, 452 tool uses, 92 min), viewer-engineer 436k (review fixes), viewer-engineer 306k (phone-fold drift fix), art-director 196k, art-director 177k, viewer-engineer 161k (CSS-leak probe), docs-steward 130k, reviewer 114k, Explore 68k, release-engineer 64k, Explore 59k, Explore 46k | ~700k: the engineer hunted ~15 page-wide CSS leaks by eye, one at a time (the first trap, third slice running; ~250k of the 733k); a first mockup that restyled today's form instead of the concept, forwarded to the owner without a concept-side check (196k); a phone-fold drift-fix pass, the third slice in a row to need one (306k) | `e2e/css-leaks.spec.ts` + allowlist: every page-wide rule reaching a ported screen is an exact list, new leak and stale entry both red; `art-director.md`: a concept mockup ships with a region-by-region GAP TABLE the coordinator reads before the owner sees a frame |
| 2026-09-08 | Two-view briefing + six-member party (ADR-0041) | ~1.75M (agents 1.54M, main ~200k est.) | viewer-engineer 368k (build, redirected 3× in flight, 65 min), combat-engineer 273k (party + 20 test files + 3 crops), art-director 219k (mockups), viewer-engineer 201k (review fixes + parking), docs-steward 160k, reviewer 132k, art-director 82k (a prompt for art that already existed), content-author 67k, content-author 39k; three agents killed by an owner interrupt at ~0 | ~330k: the build engineer was redirected three times while running because the owner's notes on the mockups arrived after the brief (~120k of rework); the first job pick (thief) was made without checking the skillset was live, and the author could not run the test that says so (~80k across a rerun and a re-brief); an art prompt was commissioned without reading the portrait README's "Wired?" column (82k); three agents killed by the stop button and re-spawned (~40k of restart) | `content-author.md`: Bash + "run the schema and party tests before you report"; root `CLAUDE.md`: collect every note on the frames before the engineer starts; the stop button kills running subagents |
| 2026-09-08 | Land the INTENT.md migration (PR #64's two commits) on main | ~85k (agents 45k, main ~40k est.) | release-engineer 45k (push OK, PR blocked by the retro gate) | ~25k: the release engineer was spawned before the retrospective ran, so it hit `require-retro-before-pr.sh` and handed back — the second slice in a row (2026-09-07, 57k) to pay for that ordering | `guard-brief.sh`: a `release-engineer` brief that names a PR is denied while `.claude/.retro-done` is missing or older than 240 min; fixture in `guard-brief.test.sh` |
| 2026-09-08 | Character dossier mockup (intent `character-dossier.md`; 12 art passes on one screen) | ~780k so far (agents ~530k, main ~250k est.) | art-director 483k over 12 resumed passes (205k first, then 8k-47k each; a fresh agent would have re-learned the file at ~150k a pass, so the resumes were the cheap path), release-engineer 48k (PR #65, carried from the prior row) | ~120k: eleven owner-note rounds arriving one per frame (each pass ~30k); one round lost to the owner reading a stale file card and asking to "REVERT" (all delivered frames shared one filename); one unmeetable alignment ask spent before the arithmetic was checked (8k) | proposed: pass-numbered frame names; check the column budget before a spacing pass; `guard-big-reads.sh` false positive on `cat >>` heredocs; correct the `.git-go` environment line |
