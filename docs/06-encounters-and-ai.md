# 06 — Encounters & AI

**Why this is a first-class doc, not a QoL bullet:** customization only *means* something against encounters that test it. Encounter design is the **demand side** of the customization economy and the **primary balancer** (`docs/04` §2). The AI is the **test harness** that decides whether a build is actually viable.

---

## 1. Encounters are the balancer

A build is "viable" only relative to what it must beat. So:
- We maintain a **benchmark encounter suite** — the fixed set the success metric runs against (`docs/00`: ≥8 archetypes clear it within the efficiency band, none dominates).
- Balance passes **re-run the suite** after any `docs/02`/`docs/07` change. If one build clears everything at top efficiency, that's a balance bug surfaced by encounters, not spreadsheets.
- **Difficulty comes from smarter enemies and better-designed maps, not stat-inflation.** (The genre's most common complaint is "hard mode = bigger numbers.")

## 2. Encounter archetypes (design vocabulary)

Each archetype stresses a different axis of the player's builds:

| Archetype | What it tests | Design levers |
|---|---|---|
| **Skirmish (open field)** | Raw build efficiency, turn economy | Symmetric-ish forces, neutral terrain |
| **Siege / choke** | Positioning, AoE, ZoC-less flow | Height, walls, one approach |
| **Defend / survive N turns** | Sustain, control, Reraise | Waves, escalating pressure |
| **Escort / protect** | Mobility, healing, aggro management | Fragile ally with its own CT turns |
| **Assassinate boss** | Burst, disable, focus-fire | High-value target + guards |
| **Elevation puzzle** | Jump/Move/Teleport, ranged height bonus | Verticality, gaps, water |
| **Anti-cheese check** | Punishes dominant strategies | Mixed damage types, status immunity pockets, dispellers |
| **Attrition / no-heal** | Resource discipline, MP economy | Long map, scarce recovery, undead |

Story battles (from the narrative repo later) are authored instances of these; the engine is data-driven so they slot in without code (`docs/05` §6, `docs/08`).

> **Implementation status (P2 Slice 3; support-aware update 2026-08-03; survive-condition update 2026-08-03).** The benchmark suite authors the archetypes expressible today — Skirmish, Siege/choke, Assassinate, Elevation, Anti-cheese, Attrition (`data/encounters/`). **The `survive` win/lose condition has now LANDED** (`src/sim/condition.ts`): `survive {teamId, ticks}` `[ENHANCEMENT]` — a team wins by keeping ≥1 unit alive until the CT clock (`state.tick`) reaches `ticks`. NOTE the field is **`ticks` (the CT clock), NOT rounds/turns**: a unit acts roughly every ~(100/Speed) ticks, so "survive ~5 rounds" at the default Speed 5 is authored as `ticks: 100`. Tag rationale: the turn/CT-bounded objective *category* is baseline (FFT has protect-until / defeat-within-N), but a pure win-BY-survival *victory* is an enhancement (canonical in FFTA/FFTA2, not our PSX baseline). Pair a survive VICTORY with an `eliminateTeams{[teamId]}` defeat (the two are exact complements at the threshold tick — no spurious draw); the symmetric `survive`-as-DEFEAT is the **defeat-within-N-ticks** objective. Authoring: set `maxTurns` generously and `maxTicks` only slightly above `ticks` so the tick cap is binding and `timeout` stays a genuine error signal (the `survive.ticks <= maxTicks` refine is a coarse guard; the `maxTurns` decision-cap is dynamic and NOT statically guarded). **What has NOT landed yet:** a **Defend benchmark ENCOUNTER** wired into the gauntlet, and measurement of the **sustain identity** it would exercise — the condition exists, but no survive-objective encounter/build is in the diversity gate yet, so Defend is not yet a measured archetype (a later slice). **Support-aware body-screening has now LANDED** (`src/sim/ai.ts` `tryScreen`, Tier A): a sturdy unit with no in-range action **interposes** on the enemy's reachable path to a **mid-charge** ally (blocking the lane via `moveRange`'s occupancy). It is a **movement fallback only** — `compareCandidate`/the action path are untouched, so a screener with any in-range action still acts. **But it did NOT unblock the glass Summoner** (the target): the summon whiffs **zero** times (the old "charge-whiff" tag was empirically wrong — ADR-0014 amendment 2026-08-03); the real blocker is that the short-range summoner **advances into melee to reach cast range and is focus-fired mid-charge**, and reachability-screening can't wall a corner-seated glass caster with **2 fillers vs 3 bruisers** on multi-lane maps (only the choke-map `enc-the-breach` holds → **1/6**). ~~So the summoner stays EXCLUDED and `N` stays 5~~ — **SUPERSEDED on 2026-08-05 (ADR-0014 amendment), still standing here until 2026-09-01.** `bld-glass-summoner` is MEASURABLE in the shipped manifest (`gauntlet.ts`) as the `summoner` identity, unblocked by a range enhancement rather than by screening, and `DIVERSITY_TARGET_N = 7`. AC-E2 seven lines below records both. The screening finding itself stands: the screening ships as **latent capability the Escort archetype needs**, regressing nothing (live gate byte-identical — the mid-charge gate means the instant-cast Coven never triggers it, so the magic axis is untouched). **Heal-in-encounter partially landed:** the balance-probe now **triages** — it heals the lowest-effective-HP ally (AC-E3(b) mirrored onto allies, ADR-0014 amendment), so a healer with a wounded ally *in range* saves the dying one first. **What Escort / Defend still need to become MEASURED archetypes:** an Escort/Defend benchmark **encounter** in the gauntlet, and — for the Summoner specifically — a **survivability lever outside the 1-ply AI** (a tankier chassis / a faster-maturing or longer-range summon that lands before contact, or multi-body wall coordination / self-preservation movement — the last reopens magic-axis recalibration). Body-screening alone is not enough; don't re-attempt the Summoner via positioning tweaks.
>
> **AoE-resolution slice (post-Slice-3).** Area abilities now resolve against every appropriate unit in their box (`aoe` threaded through the combat projection; instant + charged, targeted foes/allies, one seeded roll per target in id order). **Instant** AoE (Geomancer) lands and is measured in-suite. **Charged** AoE (Summoner) resolves correctly (proved end-to-end with the real build/AI/harness) but is **not exercised by the current suite**: the glass caster is the lowest-HP unit, so the probe focus-fires it and it dies before its slow summon matures — its damage output is unmeasured, and `abilityUsage` counts the *issued* (then cancelled) command, not landed damage. This is the **same limitation family** — a slow-setup / glass-cannon archetype. **Empirically refined (2026-08-03, ADR-0014 amendment):** protect-the-enabler body-screening was built and is **insufficient** for the Summoner (it advances into melee to reach its short cast range, then dies mid-charge; screening can't wall it 2-vs-3 on multi-lane maps). Becoming measurable needs a **survivability lever outside the AI** — a tankier chassis / a faster-maturing or longer-range summon that lands before contact — not predictive charge-targeting (0 whiffs measured) and not positioning alone. Friendly-fire AoE (the anti-clump counterplay of §4) is a deliberate later slice; today's AoE is targeted (no friendly fire).

## 3. Threat / difficulty model

- **Threat budget per encounter:** enemy count × tier × support density, tuned to a target **time-to-kill** band (`docs/07`) — not to the player's level (no mandatory scaling treadmill).
- **Difficulty tiers** (player-selectable, `docs/04` §6) adjust AI aggression/coordination and threat budget, **not** raw stat multipliers first. Stat bumps are the last lever, not the first.
- **Telegraphing:** big enemy plays (charged nukes, incoming hard-disables) are visible via the same resolution-transparency the player gets (`docs/04` §3) — fair difficulty.

## 4. AI as the test harness for builds

The AI must be good enough that **counterplay actually happens** — otherwise "power with counterplay" is a lie. Target behaviors:

- **Positioning:** use facing (attack from side/rear), height, and safe tiles; avoid clustering into player AoE; respect its own charged-spell tile targeting.
- **Focus-fire:** concentrate on the highest-value / lowest-effective-HP target; finish units in the crystal window to deny revives; punish the exposed alpha-striker (`docs/03` #2 counterplay).
- **Counter the player's build:** prefer magic vs. high-evasion physical walls; use Silence vs. casters, Dispel vs. self-buffers, status that ignores Faith vs. low-Faith tanks; disable the enabler (the Haste-bot, the summoner) first.
- **Resource sense:** don't waste charged nukes on a unit that will walk away; use terrain/elements; retreat/reposition when losing tempo.
- **Threat assessment:** value disables and tempo, not just damage (mirror the player's tools).

**Determinism:** all AI decisions draw from the seeded RNG in declared order (`docs/05` §3) so battles stay reproducible and rewind-safe. Tie-broken decisions must be deterministic.

## 5. Anti-degenerate guarantees (encounter-side)

- **No mandatory grind:** encounters give bounded, non-RNG-treadmill rewards (`docs/07`); you never *must* farm randoms to progress.
- **Anti-cheese encounters** exist specifically to answer dominant strategies (mixed damage, immunity pockets, dispellers) — the structural check on stun-lock, evasion-stacking, and Blade-Grasp walls (`docs/04` §1, §5).
- **Stun-lock cap `[NOT BUILT]`:** ~~repeated hard-disables on the same unit hit diminishing returns (implemented as a status rule, `docs/05`)~~ — **there is no such status rule** (checked 2026-09-01). `resolve.ts` `applyInflicts` REFRESHES an existing status to the longer of the two remaining lifetimes, so a re-applied Stop or Don't-Act costs the victim full duration again; nothing decays with repetition. Neither AI nor player is bounded here. The intent is kept as `docs/07` **AC-P7**, which is explicitly aspirational.

## Acceptance Criteria (SDD-ready)

- **AC-E1 (benchmark exists):** A named benchmark suite covering the §2 archetypes SHALL exist and be runnable headlessly for balance verification.
- **AC-E2 (diversity gate):** On the benchmark suite, ≥ 8 `docs/03` archetypes SHALL clear within the efficiency band and no single build SHALL clear all encounters at top efficiency. **Phased (ADR-0014):** the greedy 1-ply probe can measure a *distinct identity* for **6** of the shipped archetypes today (`aim.`, `black-magic.`, `geomancy.`, `punch-art.`, `summon.`, `white-magic.` — `spellblade` and `arcane-artillery` share the `black-magic.` prefix, so those two collapse to one; the rest field as masked brawlers or need capabilities that don't exist — provoke, reaction-as-live). The CI gate enforces `distinctMeasurableArchetypes ≥ N` (**N=7 now — see the CONTROL-slice note at the end of this criterion**) **plus** a relative, threshold-free dominance ban (a build fails only if it clears all six gauntlet maps and no other measurable build clears any faster), with a committed `MEASURABLE`/`EXCLUDED` manifest naming each blocker. **Support-aware AI slice (landed, ADR-0014 amendment 2026-08-03):** a **heal-triage** refinement (the probe heals the lowest-effective-HP ally — AC-E3(b)'s focus rule mirrored onto allies — not the biggest overheal) shipped, and `bld-reraise-cleric` moved EXCLUDED→MEASURABLE as the **`white-mage`** identity (its *offensive* white-magic via `holy`; it was mis-excluded — only its unmodeled reaction/sustain identity was blocked). ⚠ **N=6 is not a durable floor:** **two** of the six prefixes are **contingent on MP being unenforced** — `white-magic.holy` (56 MP vs the cleric's 24) and now `summon.*` (14–30 MP vs the summoner's 24; the sim does not check MP yet). When MP enforcement lands both could regress and N could drop **6→4**; uniquely among the phased capabilities, MP enforcement *lowers* N. **`glass-summoner` moved EXCLUDED→MEASURABLE (ADR-0014 amendment 2026-08-05)** as the **`summoner`** identity via a range `[ENHANCEMENT]` (offensive `summon.*` h4→h6) that lets the glass caster open `summon.` from its backline instead of advancing into melee and dying mid-charge (the re-diagnosed *structural-survivability* blocker, not charge-whiff — 0 whiffs measured). `battle-cleric` (prefix collapse) stays EXCLUDED with its refined tag. **Multi-matchup opposition (landed, ADR-0014 amendment):** the gauntlet fields each candidate against an `OPPOSITIONS` set of distinct *threat* profiles — `phys` (the reference bruiser team) + `magic` (the Coven: instant Faith-scaling geomancy) — so threat, not just geometry, varies. Opportunity cost is real and *surfaced* per build (`losingMatchups`); the roll-call moves with every re-tune, so read it from a live `npm run state` rather than from this line — as of 2026-08-12 `reraise-cleric` and `glass-summoner` fold to magic while `terrain-geo`, `longshot` and `faithzero-monk` clear both axes (a non-uniform matrix). **Scope honesty:** the diversity count still keys on the `phys` **reference** opposition only (a build is *supposed* to lose to some threat, so requiring viability everywhere would collapse the count), so a magic regression is *surfaced* (`losingMatchups`/`noLosingMatchup`), not enforced — the sole hard fail stays relative dominance (now over `{maps × oppositions}`). The magic axis is a second distinct threat, **not** an isolated candidate-Faith test: shared Faith-50 filler allies + team-elimination + magnitude-focus targeting mean it rewards tempo/range as much as personal Faith (so the anti-mage build still shows a *team*-level magic loss — its personal resistance can't save its allies without support-aware positioning); the isolated Faith cliff is proved by a must-fail straddle test. Anti-convergence is thus *tested and surfaced*, not yet auto-enforced; `≥ 8` remains the release bar and `N` rises only as a new signature prefix (job/skillset) or those capabilities land. ⚠ **N RE-BASELINED TWICE ON 2026-08-12: 6 → 1 → 5.** Teaching the balance probe ADR-0015's move+act fold (one turn may move *and* act, settling at −100) dropped six of seven candidates just under `VIABLE_MIN_MAPS`=4, leaving one identity (`black-magic.`). **The follow-up diagnosis found the fold was not the operative cause (ADR-0016).** Every shipped build died to ONE basic attack — a 72-HP knight vs a 90-damage swing — so fights lasted 2–4 turns and were settled by turn order, making range, positioning, tempo and signature abilities all invisible. `docs/07` §3 had specified the intended pacing all along ("a squishy unit dies in ~1–2 committed actions; a tank in ~3–4") but nothing tested it, so the content missed it by 3–4× unnoticed. **The fix is content, symmetric, and applied to BOTH sides** — `raw.hp` re-authored across all 14 builds into the docs/07 band, now enforced by `docs/07` **AC-P6** (`src/sim/ttk.test.ts`). A second, dependent fix was forced by the first: `magicDamage` applies Faith at both ends, so at the shared Faith 50/50 every magic ability is quartered while physical ones are not — and geomancy's `power` had been authored on the physical scale, leaving a geomancer's spell (45) at half its own punch (80). The probe therefore punched, and `terrain-geo` won its maps *as a knight* (the masking class). Geomancy power ×~2.7 fixes it, and also restores the **magic threat axis**, which had gone toothless against 315-HP bodies. Measured after: `faithzero-monk` 6/6, `terrain-geo` 6/6, `longshot` 5/6, `glass-summoner` 5/6, `reraise-cleric` 4/6 → **N=5**, robust at 5–6 across a ±15% HP perturbation. **The one identity still missing is `black-magic.`**: `arcane-artillery` is too fragile to land four casts, and `bld-spellblade` is MASKED (a knight's MA 6 makes borrowed magic lose to its own swing) — asserted positively in `ttk.test.ts` so it cannot be forgotten. `geomancy.*` is BACK in the as-authored benchmark suite's required-skillset list, the deliberate update its exactly-zero pin was designed to force. `≥ 8` is unchanged as the release bar. ⚠ **N RE-BASELINED A THIRD TIME ON 2026-08-12: 5 → 6 (ADR-0017, the support slot).** The missing `black-magic.` identity was recorded above as a tuning problem — a caster too fragile to land four casts. **Tracing a losing run said otherwise, and the recorded cause was wrong.** The wizard declares 17 charges across the six gauntlet maps: 8 land, 4 whiff, and **5 are cancelled by its own death mid-charge**; the discriminating comparison is `glass-summoner`, which is *squishier* (134 HP) and casts *three times slower* (10 ticks vs 3) yet stays viable, because range 6 keeps it out of reach where range 5 does not. The cause underneath was not balance at all: `build.ts` projected **action** abilities only, so the **reaction / support / movement** slots were validated at equip time and then ignored — **nine of fourteen shipped builds carried a dead support slot**, `bld-arcane-artillery` among them, and its whole caster identity rides on an equipped Magic Attack Up that had never applied. The five-slot ability chassis is a locked decision (`docs/02` §A2) with three decorative slots, so every build leaning on one measured as under-tuned content. **What shipped:** an equipped-**support** modifier layer (`src/sim/support.ts`), applied `growth → trait → support → clamp`, with effects authored as data on the ability; the four effects expressible with currently-modeled stats are live (`magic-attack-up` ma ×1.33, `brawl-up` pa ×1.25, `charge-time-down` ability-speed ×2, `range-up` ability-range +1) and the other four are listed in `DEFERRED_SUPPORT_EFFECTS` with a named blocker, the `EXCLUDED` pattern. **⚠ That 4-live/4-deferred split is the ADR-0017 slice's record and no longer describes the pack.** Re-counted 2026-09-01: the pack holds 12 support abilities — ~~four~~ eight live, four deferred. Live: `punch-art.martial-arts` (pa ×1.25), `battle-skill.hp-boost` (maxHp ×1.2), `black-magic.magic-attack-up` (ma ×1.33), `geomancy.attunement` (pa +1, ma +1), `white-magic.short-chant` (chargeSpeed ×1.25), `summon.short-charge` (chargeSpeed ×2), `aim.steady-aim` and `aim.eagle-eye` (ability range +1). Deferred: `battle-skill.equip-heavy-armor`, `steal.secret-hunt`, `aim.concentrate`, `summon.arcane-focus`. And **`brawl-up`, `charge-time-down` and `range-up` are not ability ids** — no such ids exist in `data/base-pack.json`; they name the effects now carried by `punch-art.martial-arts`, the two short-charge supports and the two `aim.` range supports. `support.test.ts` partitions the pack exactly, so the counts here are the stale half, not the code. Measured: `arcane-artillery` 1/6 → **5/6 with `black-magic.` landed on all five**, `reraise-cleric` 4/6 → 6/6, `dominantBuilds` still empty, `noLosingMatchup` unchanged. **Robustness (the ADR-0016 protocol):** the *pre*-fix baseline reads N = 6, 6, **5**, 5, 5, 6 across a ±15% HP perturbation — the wizard was straddling a discontinuity, not stably sub-viable, which is exactly why the arithmetic diagnosis and the sim disagreed; *with* the layer it holds 5/6 flat and N stays 6 across ±10%. ×1.33 is on that plateau rather than tuned to it (×1.20 collapses again at +10% HP) and is FFT's own Magic Attack UP figure `[UNCERTAIN — verify vs BMG]`. **Rejected and recorded** (`docs/plans/slice-black-magic-carrier.md`): raising the wizard's HP works but puts it in `noLosingMatchup` above +10% HP (the B5 convergence failure); raising `black-magic` **range** reaches 6 through the still-masked `bld-spellblade`, which lands its signature on exactly one of four clears — the gate's `≥ 1 signature` interim-floor insensitivity being exploited, not a fixed build. **Zero slack now:** all six expressible prefixes count, so any regression fails the gate, and the MP contingency above is unchanged. `bld-arcane-artillery` is also now an as-authored occupant of `enc-mixed-company` (3v3 where it was 2v3, so that encounter's play shifts) — without it no benchmark encounter fielded a black-mage at all, which was the *real* reason `black-magic` was absent from AC-E1's required-skillset list. ⚠ **N RAISED 6 → 7 ON 2026-08-16 (ADR-0018, the CONTROL slice).** `steal.` joins as a genuinely new signature prefix, carried by a new build `bld-cutpurse` — the first identity whose signature action deals **no damage at all**: `steal.heart` charms, and a charmed unit fights for its captor (`docs/01` §8). Three capabilities that each *read* as working had to land together for it to be measurable: the balance probe now prices an inflicted status in **HP-equivalent** and folds it into `magnitude` (deliberately **not** a `DISABLE` action class, which would have overridden AC-E3(b)'s focus rule), Charm has **behaviour** via a single allegiance seam (`effectiveTeamOf`, schema v9→v10), and the gate's `inBand` contribution test counts a **landed status** — under the damage-or-healing-only test a pure-control build scored zero contribution on every map it decided, so the gate could never have credited one. **A livelock had to be fixed first, and a non-monotonic sweep is what exposed it:** the cutpurse first measured 3, 4, 3, 4 across adjacent Speed values, because victory counted a charmed body for its *nominal* team — so charming the last defender froze the battle (nobody attacks an ally; the charmer re-charms the moment control lapses), a 580-tick timeout with 18 charms landed. Victory now counts the team a unit **fights for**, symmetrically, and the jitter vanished: **4/6 flat across speeds 8–11 and across a common raw-HP perturbation of ×0.95…×1.15, N=7 at every step** (it drops out at ×0.90). The cutpurse sits **exactly at `VIABLE_MIN_MAPS` with `losingMatchups: ["magic"]`, by design** — at raw Speed 7 it reads 6/6 with *no* losing matchup, the B5 convergence failure — so its raw Speed is the roster's uniform 8 and the archetype's quickness comes from job growth. **Decision 1 changed ZERO shipped runs on its own** (A/B over the whole gauntlet, every ability-usage histogram byte-identical): `magnitude` is only the second comparator key, so it decides among acts on the *same* focus target, and the shipped inflicting abilities were already the biggest hit there. `≥ 8` remains the release bar; `steal`'s other four actions stay inert behind the economy and equipment layers, now tracked per-ABILITY in `DEFERRED_ACTIONS` rather than per-skillset. ⚠ **N UNCHANGED AT 7 ON 2026-08-18 (ADR-0019, the REACTION slice) — and that is the honest result, not a shortfall to explain away.** The reaction chassis slot went live: `punch-art.counter` and `punch-art.hamedo` now fire from the equipped slot at Brave%, so `bld-counter-wall` moved EXCLUDED→MEASURABLE with its named blocker ("reaction-as-live modeling") genuinely retired — it clears **6/6** phys maps with its signature landed on every one. It raises the count by **zero**, because `punch-art.` is already carried by `bld-faithzero-monk`: a second CARRIER, not a new identity, the same collapse `bld-spellblade`/`bld-arcane-artillery` show on `black-magic.`. **A retired blocker does not owe the count a point**, and the gate now asserts that in both directions — dropping *all* carriers of a prefix costs one identity, dropping *one* of two costs none. The capability itself is real and measurable: an A/B over the whole gauntlet with the effects stripped moves **13 of 96** runs (the monk's `hpFraction` and signature-landed both rise on Hamedo; the wizard's `candidateDamage` rises 340→380 on Counter). `dominantBuilds` stays empty; `winsAllInBand` gains `bld-counter-wall` alongside the monk — two builds now sweep every cell without outclassing the field, which is a design conversation to watch, not a gate failure. `bld-aggro-tank` stays EXCLUDED with a **re-measured** tag: it is no longer blocked from being *measured* (it clears 6/6 too) but from being **distinct** — its rows are byte-identical to `bld-counter-wall`'s, the two differing only in raw HP (255 vs 242) and trait order, neither of which mattered in any shipped run. **`≥ 8` still needs a new signature prefix**, and with jobs deprioritised the remaining EXCLUDED entries cannot supply one. ⚠ **N HELD AT 7 ON 2026-08-20 (ADR-0020, the MOVEMENT + EXPOSURE slice), and the interesting result is what it took to HOLD it.** The last inert chassis slot was authored as a real +2 Move and the count fell **7 → 5**: `black-magic.` and `summon.` both lost, `arcane-artillery` and `glass-summoner` 5/12 → 1/12, `spellblade` 6 → 2, melee flat or slightly worse, **no build improved anywhere**. The fold was not at fault. `compareCandidate` enumerated every reachable tile, priced the ACT available from each, and had **no term for the tile's exposure** — so low Move had been keeping fragile builds alive *by accident*, and extra Move was pure rope (traced: at Move 3 the wizard casts from behind its own line and wins in 19 turns; at Move 5 it walks into two enemies' strike envelope and dies mid-charge). **Three fixes were measured, not argued:** "prefer the nearest tile among equal acts" → still 5 (nearest is not safest); "stay put unless you cannot act at all" → still 5 **and** it tripped the dominance ban; **"prefer the tile fewest living foes can strike" → 7, with Move +2 live.** The shipped term is AC-E3(d), and its POSITION in the key sequence was calibrated across six placements (two work): it sits **below** the AC-E3(b) focus keys so the focus rule is untouched, and above `facingRank`. The aggressive placement also reaches 7 but overrides a documented AC and measures worse on anti-convergence. **Robustness:** N=7 flat across raw-HP ×0.95…×1.15 (dropping to 6 at ×0.90, exactly as the *pre-slice* baseline does) — a plateau, not a knife-edge, and the same plateau the baseline sits on. **Anti-convergence IMPROVED:** `winsAllInBand` 2 → **1** (`bld-faithzero-monk` alone), because a candidate that no longer walks into the open loses cells it used to sweep; `dominantBuilds` stays empty; timeouts 1 → **0**. **`bld-spellblade` IS NO LONGER MASKED** — the mask pinned since the TTK re-tune. Its magnitude cause is unchanged and still asserted (`ttk.test.ts`: borrowed black magic 37 < its own knight swing 90); what changed is that from the tiles a cautious probe now prefers, the melee swing is **out of reach**, so range-5 black magic is what is left. It lands `black-magic.` on every map it is in band for, and is now honestly **sub-viable** (3/6 vs a floor of 4) rather than masked — a content gap, not an AI one. **The exposure term moved 36 of 108 gauntlet runs on unchanged content while every aggregate verdict stayed identical** — a live capability that moves no summary, exactly the reading the root CLAUDE.md warns about.
- **AC-E3 (AI counterplay):** Enemy AI SHALL demonstrably (a) attack from side/rear when reachable, (b) focus the lowest-effective-HP valid target, (c) select damage/status types that counter the opposing unit's defenses, and (d) **among tiles that yield the same act, resolve it from the one fewest living foes can strike** — reach measured as each foe's Move plus its weapon range — in scripted test scenarios. *Discriminator for (d):* a fixture where the safest tile is neither the actor's current tile nor the nearest one, so the actor must move BACKWARD to act; "stay put" and "prefer the nearest tile" were both measured against the gate and both failed (ADR-0020). (d) sits BELOW (b) in the ranking, so it never overrides the focus rule — it decides only where the act is made from.
  > **(c) is PARTIALLY MET, and no test covers it (verified 2026-09-01).** `src/sim/ai.ts` says so at the source: `effHp = hp`, with no mitigation-aware (Protect / evasion / element) target selection, so the probe never "selects damage/status types that counter the opposing unit's defenses". (a) and (b) have tests named for them in `ai.test.ts`/`aoe.test.ts`; (d) is covered by the exposure block at `ai.test.ts:694+` (titled for ADR-0020, and it asserts `exposureOf` picks the minimum among tiles that genuinely differ). **(c) has nothing.** Read AC-E3 as three-quarters asserted. Closing (c) needs a mitigation term in `compareCandidate` plus a scripted fixture where the countering choice and the raw-magnitude choice disagree — without that disagreement the fixture passes under both policies.
- **AC-E4 (determinism):** Given a fixed seed and player command log, AI decisions SHALL be reproducible (draws from the seeded stream only).
- **AC-E5 (difficulty without inflation):** Raising a difficulty tier SHALL first change AI behavior/threat budget; raw stat multipliers SHALL be a bounded, secondary lever.
- **AC-E6 (every shipped encounter is winnable):** Each `data/encounters/enc-*.json` SHALL be able to reach its **victory** condition for team 0 — not merely a decisive outcome (AC-E1), which a loss also satisfies. The check runs each encounter with the opposition's decisions replaced by `wait`, and requires `outcome === "victory"`. *Discriminator:* it fails on the real structural defects — a victory target that cannot be reached or damaged (`enc-behead-the-warlord`'s `defeatUnit`), a roster whose abilities land nothing, a condition naming a unit that does not exist — none of which AC-E1 can see, because timeout and defeat both read as "the battle ended". *Non-degeneracy guard:* the same encounter run **as authored** SHALL leave team 0 at a strictly lower HP fraction than the passive run. Without it, an injected decider that was silently ignored would give two identical runs and the criterion would certify nothing.
  > **AC-E6 is a REACHABILITY criterion, not a balance one.** It says the win condition is achievable; it does not say a player is likely to win, and it must never be read as one. **Measured 2026-08-18, and deliberately NOT asserted** (asserting it would freeze a tuning state): run as authored with the balance probe on both sides, all five encounters end in **defeat** for team 0. Sweeping 25 seeds, `enc-the-high-ground` wins 10, `enc-the-long-march` 3 and `enc-the-breach` 1; `enc-mixed-company` and `enc-behead-the-warlord` win **0 of 200**, and in the latter the warlord never drops below **86%** HP because nothing in the greedy probe prefers the objective target. Whether that is a content problem or an AI one is an open question for a later slice — AC-E6 only forecloses the third possibility, that the encounters cannot be won at all.
